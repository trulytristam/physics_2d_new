use crate::engine::collisions::gjk::Simplex;
#[cfg(test)]
mod tests {
    use crate::engine::collisions::gjk::Simplex;

    // A test function
    #[test]
    fn test_push_rearrange() {
        
        let s = Simplex::default();

        
    }
}
